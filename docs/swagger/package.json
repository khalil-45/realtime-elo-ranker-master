{
  "name": "swagger",
  "version": "1.0.0",
  "scripts": {
    "start": "cross-env LIVERELOAD_PORT=35729 nodemon --watch swagger.yaml index.mjs"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "nodemon": "^2.0.15"
  }
}